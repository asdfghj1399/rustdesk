name: "1️⃣ Windows EXE Builder (Fixed)"

env:
  ACTIONS_ALLOW_UNSECURE_COMMANDS: true  # 允许旧版命令
  VCPKG_DISABLE_METRICS: true           # 禁用vcpkg遥测

on:on:
  # 手动触发（带参数）
  workflow_dispatch:
    inputs:
      build_type:
        description: "Build Type (Debug/Release)"
        required: true
        default: "release"
        type: choice
        options: 
          - "release"
          - "debug"
  
  workflow_dispatch:
    inputs:
      build_type:
        description: "Build Type"
        required: true
        default: "release"
        type: choice
        options: ["release", "debug"]
  
  push:
    branches: [master]

jobs:
  build:
    name: "🛠️ Build (${{ github.event.inputs.build_type || 'release' }})"
    runs-on: windows-latest

    steps:
    # 第一步：检出代码
    - name: "1️⃣ Checkout Code"
      uses: actions/checkout@v4

    # 第二步：安装Rust
    - name: "2️⃣ Install Rust"
      uses: dtolnay/rust-toolchain@v1
      with:
        toolchain: stable
        targets: x86_64-pc-windows-msvc

    # 第三步：安装vcpkg（修复版）
    - name: "3️⃣ Install vcpkg"
      shell: pwsh
      run: |
        $vcpkgRoot = "C:\vcpkg"
        if (-not (Test-Path "$vcpkgRoot\.git")) {
          git clone https://github.com/microsoft/vcpkg $vcpkgRoot
        }
        cd $vcpkgRoot
        git pull
        .\bootstrap-vcpkg.bat -disableMetrics
        echo "VCPKG_ROOT=$vcpkgRoot" >> $env:GITHUB_ENV
        echo "$vcpkgRoot" | Out-File -FilePath $env:GITHUB_PATH -Append

    # 第四步：安装依赖
    - name: "4️⃣ Install Dependencies"
      shell: pwsh
      run: |
        $ErrorActionPreference = "Stop"
        & "$env:VCPKG_ROOT\vcpkg" install `
          --triplet x64-windows `
          --clean-after-build `
          libvpx libyuv openssl

    # 第五步：构建项目
    - name: "5️⃣ Build Project"
      shell: pwsh
      run: |
        $buildType = "${{ github.event.inputs.build_type || 'release' }}"
        cargo build --$buildType --target x86_64-pc-windows-msvc
        
        $exePath = "target\x86_64-pc-windows-msvc\$buildType\rustdesk.exe"
        if (-not (Test-Path $exePath)) {
          Write-Output "::error::EXE not found at $exePath"
          Get-ChildItem -Recurse target\
          exit 1
        }
        echo "EXE_PATH=$exePath" >> $env:GITHUB_ENV

    # 第六步：上传产物
    - name: "6️⃣ Upload Artifact"
      uses: actions/upload-artifact@v4
      with:
        name: "rustdesk-${{ github.event.inputs.build_type || 'release' }}"
        path: |
          ${{ env.EXE_PATH }}
          target\x86_64-pc-windows-msvc\${{ github.event.inputs.build_type || 'release' }}/*.dll
        retention-days: 7
        if-no-files-found: error
